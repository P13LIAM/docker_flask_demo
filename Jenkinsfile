pipeline {

    agent {

        node {
            label "jenkins_agent"
        }
    }

    stages {

        stage("build") {
            echo "building huhu"
        }

        stage("test") {
            echo "testing huhu"
        }

        stage("deploy") {
            echo "deploying huhu"
        }
    }
}